# MCP 服务器配置指南

## 📋 概览

本知识库系统集成了 Model Context Protocol (MCP) 服务器，提供外部工具集成能力，实现移动端访问和实时搜索功能。

## 🔧 已配置的服务器

### 1. Filesystem MCP ✅

**状态**: 已启用
**用途**: 增强的文件系统访问
**路径**: `/Users/zhangyuhang/knowledge-base`

**功能**:
- 读取和写入文件
- 目录列表和搜索
- 文件元数据访问

---

### 2. GitHub MCP 🔧

**状态**: 未启用（需要配置）
**优先级**: **高**（移动端工作流必须）
**用途**: 远程控制 GitHub 仓库

**配置步骤**:

#### 2.1 生成 GitHub Personal Access Token

1. 访问: https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 设置权限:
   - ✅ `repo` (完整仓库访问权限)
   - ✅ `issues` (创建和管理 issues)
   - ✅ `pull requests` (创建和管理 PRs)
4. 生成并复制 token

#### 2.2 配置环境变量

**方法 1: 临时设置（当前会话）**
```bash
export GITHUB_TOKEN="your_token_here"
```

**方法 2: 永久设置（推荐）**

在 `~/.zshrc` 或 `~/.bashrc` 中添加:
```bash
export GITHUB_TOKEN="your_token_here"
```

然后重新加载配置:
```bash
source ~/.zshrc
```

#### 2.3 启用服务器

编辑 `.claude/mcp/servers.json`:
```json
"github": {
  "enabled": true,  // 改为 true
  // ...
}
```

#### 2.4 验证配置

```bash
claude mcp list
```

应该看到 `github` 在列表中。

---

### 3. Perplexity MCP 🔍

**状态**: 未启用（可选）
**用途**: 实时搜索最新信息

**配置步骤**:

1. 访问: https://www.perplexity.ai/settings/api
2. 生成 API Key
3. 设置环境变量:
   ```bash
   export PERPLEXITY_API_KEY="your_key_here"
   ```
4. 在 `servers.json` 中设置 `enabled: true`

**使用场景**:
- 搜索最新技术文档
- 查找学习资源
- 获取实时信息

---

### 4. Brave Search MCP 🔍

**状态**: 未启用（可选）
**用途**: 备用搜索引擎

**配置步骤**:

1. 访问: https://api.search.brave.com/app/keys
2. 获取 API Key
3. 设置环境变量:
   ```bash
   export BRAVE_API_KEY="your_key_here"
   ```
4. 在 `servers.json` 中设置 `enabled: true`

---

### 5. SQLite MCP 💾

**状态**: 未启用（可选）
**用途**: 结构化数据存储

**使用场景**:
- 学习数据统计
- 复习历史记录
- 知识图谱查询

**配置步骤**:

1. 创建数据库文件（可选）:
   ```bash
   touch /Users/zhangyuhang/knowledge-base/knowledge.db
   ```

2. 在 `servers.json` 中设置 `enabled: true`

---

## 📱 移动端工作流设置

### 概览

通过 GitHub Issues + GitHub MCP，可以在移动设备上远程控制 Claude Code。

### 工作流程

```
移动设备 → GitHub Issues → GitHub MCP → Claude Code → 执行任务 → 提交结果
```

### 详细步骤

#### 步骤 1: 推送到 GitHub

```bash
# 添加远程仓库（如果还没有）
git remote add origin https://github.com/YOUR_USERNAME/knowledge-base.git

# 推送所有内容
git push -u origin main
```

#### 步骤 2: 在移动设备上创建任务

**使用 GitHub Mobile App**:

1. 打开 GitHub app
2. 进入你的知识库仓库
3. 点击 "Issues" → "+"
4. 创建新 issue:

**标题**: "学习 React Server Components"

**描述**:
```
请帮我学习 React Server Components 并创建知识条目。

需要包含:
- 核心概念
- 与传统组件的区别
- 使用示例
- 最佳实践
```

5. 在评论中触发执行:
   ```
   @claude 请开始执行这个任务
   ```

#### 步骤 3: Claude Code 执行

在桌面端，Claude Code 会:

1. GitHub MCP 接收到 issue 通知
2. 解析任务需求
3. 执行 `/learn react-server-components`
4. 创建知识条目
5. 更新 issue 状态

#### 步骤 4: 查看结果

**在 Obsidian Mobile 中**:

1. 打开 Obsidian app
2. Git 同步（下拉刷新）
3. 查看新创建的知识条目
4. 添加个人笔记

**在 GitHub App 中**:

1. 查看 issue 更新
2. 阅读执行结果
3. 添加反馈或新任务

---

## 🔍 MCP 命令参考

### 列出已安装的服务器

```bash
claude mcp list
```

### 测试服务器连接

```bash
claude mcp test github
```

### 添加新服务器

```bash
claude mcp add github --scope user -- npx -y @modelcontextprotocol/server-github
```

### 移除服务器

```bash
claude mcp remove github
```

---

## 🚨 故障排除

### GitHub MCP 不工作

**问题**: GitHub MCP 无法连接

**解决方案**:

1. 检查 token 是否有效:
   ```bash
   curl -H "Authorization: token $GITHUB_TOKEN" https://api.github.com/user
   ```

2. 检查 token 权限:
   - 确保 `repo` 权限已启用
   - 重新生成 token（如果需要）

3. 验证环境变量:
   ```bash
   echo $GITHUB_TOKEN
   ```

### Perplexity MCP 返回错误

**问题**: API 调用失败

**解决方案**:

1. 检查 API Key 是否有效
2. 确认账户余额
3. 验证环境变量设置

### Filesystem MCP 权限错误

**问题**: 无法访问文件

**解决方案**:

1. 检查目录路径是否正确
2. 确认文件权限:
   ```bash
   ls -la /Users/zhangyuhang/knowledge-base
   ```

3. 必要时调整权限:
   ```bash
   chmod -R 755 /Users/zhangyuhang/knowledge-base
   ```

---

## 📊 使用场景

### 场景 1: 通勤中学习

**时间**: 地铁上 30 分钟

**操作**:
1. 在 GitHub app 创建 issue: "学习 Rust 所有权"
2. 评论: "@claude 请创建学习计划和知识条目"
3. 到达办公室时，知识已准备好

**结果**: 通勤时间不浪费，主动学习

---

### 场景 2: 床上复习

**时间**: 睡前 20 分钟

**操作**:
1. Obsidian mobile 查看今日复习提醒
2. 阅读 2-3 个知识条目
3. 添加笔记和思考
4. Git 同步

**结果**: 巩固记忆，持续进步

---

### 场景 3: 灵感记录

**时间**: 随时

**操作**:
1. 语音备忘录转文字
2. 发送到 GitHub issue
3. 评论: "@claude 请整理这些想法"
4. 桌面端自动分析和整理

**结果**: 捕捉灵感，不丢失想法

---

## ⚙️ 最佳实践

1. **Token 安全**
   - 不要提交 token 到代码库
   - 使用环境变量
   - 定期轮换 token

2. **权限最小化**
   - 只授予必要的权限
   - 使用不同 token 用于不同用途

3. **监控使用**
   - 定期检查 API 使用量
   - 监控费用
   - 设置预算提醒

4. **备份策略**
   - 定期提交到 GitHub
   - 使用 Git 标签标记重要版本
   - 保持多个备份

---

## 📚 相关资源

- [MCP 官方文档](https://modelcontextprotocol.io/)
- [GitHub MCP 文档](https://github.com/modelcontextprotocol/servers)
- [Perplexity API 文档](https://docs.perplexity.ai/)
- [Claude Code MCP 指南](https://code.claude.com/docs/mcp)

---

## 🎯 下一步

1. ✅ 配置 GitHub MCP
2. ✅ 推送代码到 GitHub
3. ✅ 设置 Obsidian mobile
4. ✅ 测试移动端工作流
5. ✅ 开始实际使用

---

**配置日期**: 2025-12-24
**MCP 版本**: 1.0
**状态**: 待配置
